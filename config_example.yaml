model:
  d_model: 128
  num_heads: 4
  d_ff: 512
  num_layers: 4
  max_seq_len: 128
  dropout: 0.1

train:
  batch_size: 64
  lr: 0.0001
  weight_decay: 0.01
  epochs: 10
  checkpoint_path: checkpoints/model.pt

test:
  checkpoint_path: checkpoints/model_epoch6.pt